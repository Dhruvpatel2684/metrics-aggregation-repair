[metadata]
author_name = "Dhruvkumar Patel"
author_email = "dhruvpatel2284@gmail.com"
category = "Graph Algorithms"
difficulty = "hard"
tags = ["dependency-resolution", "topological-sort", "graph-traversal", "level-assignment", "constraint-propagation"]

[task]
name = "depgraph-topo-repair"
description = "Fix a dependency graph topological resolution system that produces incorrect level assignments, missing source registrations, inflated visit counts, and unstable sort ordering."

[environment]
dockerfile = "environment/Dockerfile"
runtime_dir = "environment/runtime"

[testing]
test_script = "tests/test.sh"
test_file = "tests/test_resolver.py"
total_tests = 10
passing_without_fix = 4

[solution]
solve_script = "solution/solve.sh"
repair_file = "solution/repair_resolver.py"
